<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Electron/V8 need a writable+executable JIT heap. -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <!-- Electron reads env vars (e.g. injected PATH) at launch. -->
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>
  <!-- Maestro loads native modules (node-pty, better-sqlite3) from asarUnpack. -->
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <!-- Microphone access for on-device voice dictation (the bundled
       maestro-dictation helper inherits these entitlements). -->
  <key>com.apple.security.device.audio-input</key>
  <true/>
</dict>
</plist>
